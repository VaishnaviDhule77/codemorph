# CodeMorph runtime dependencies.
#
# Phases 1-5 (analysis, findings, CFG/data-flow, deterministic migration,
# sandboxed verification) run on the Python standard library alone
# (ast, subprocess, dataclasses, ...). Dependencies for later phases
# (FastAPI, LLM HTTP client) will be added when those phases land --
# no speculative dependencies.