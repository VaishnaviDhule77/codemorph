# CodeMorph runtime dependencies.
#
# Phases 1-7 (analysis, findings, CFG/data-flow, deterministic migration,
# sandboxed verification, equivalence, LLM integration) run on the Python
# standard library alone (ast, subprocess, urllib, ...). Dependencies for
# later phases (FastAPI, frontend tooling) will be added when those phases
# land -- no speculative dependencies.
